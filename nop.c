#include "monty.h"

/**
 * nop - does nothing
 * @stack: pointer to stack
 * @line_number: the file line where instructions occur
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
